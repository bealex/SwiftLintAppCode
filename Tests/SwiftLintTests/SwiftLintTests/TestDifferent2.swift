private class TestDifferent2 {
    var sssx: Int = 0
    @objc func f() {}

    let foo: (start: Int, end: Int, value: String)

    func fooo() {
        let foo = bar.filter(toto)
                .joined(separator: "")

        expect(12).toNot(equal(10))

        CGRectGetHeight(rect)

        NSZeroPoint

        CGPointMake(xVal, yVal)

        NSWidth(rect)

        let x = [
            key: value
            ]

        foo(0,
                param1: 1, param2: true, param3: [3])

        foo.something(param1: { $0 }) { $0 + 1 }

        // TODO: Finished: No Extension Access Modifier
    }

    func foo(_ param1: Int,
             param2: Int, param3: Int) -> (Int) -> Int {
        return { xxx in xxx + param1 + param2 + param3 }
    }

  // a
}

public class AAAAA { class BBBB { class CCCC {} } }
